const parseArgs = () => {
     const argumentsArray=process.argv.slice(2)
     for(let i=0;i<argumentsArray;i++){
        if(argumentsArray[i].startsWith('--')){
            
        }
        else{

        }
     }
};

parseArgs();